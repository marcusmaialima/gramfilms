import styled from 'styled-components';
import logo from '../../assets/img/logo.png'

const LogoGramFilms: string = styled.img.attrs({ src: logo, alt: 'Imagem da logo GramFilms' })`
    height: 40px;
`

export default LogoGramFilms